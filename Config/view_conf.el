;; view Mode
;; (add-hook 'view-mode-hook 
;;           (lambda () (local-set-key (kbd "j") 'View-scroll-line-forward)))
;; (add-hook 'view-mode-hook 
;;           (lambda () (local-set-key (kbd "k") 'View-scroll-line-backward)))
